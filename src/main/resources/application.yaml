spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/task-manager-poc
    username: local
    password: local
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080

logging:
  level:
    org:
      springframework:
        web: info
